export enum TestMessagingStream {
  TEST_STREAM = 'stream:test',
}

export enum TestMessagingGroup {
  TEST_GROUP = 'group:test',
}

export enum TestMessagingConsumer {
  TEST_CONSUMER = 'our-consumer',
}

export enum TestEventId {
  DEFAULT = 'evt-123',
  MSG_PENDING = '1-0',
  MSG_CLAIMABLE = '2-0',
  MSG_OWN_PENDING = '3-0',
}
